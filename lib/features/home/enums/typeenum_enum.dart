import 'package:flutter_restaurant/common/models/enum_values_model.dart';

enum TypeEnum {
  MULTI,
  SINGLE
}

final typeEnumValues = EnumValuesModel({
  "multi": TypeEnum.MULTI,
  "single": TypeEnum.SINGLE
});