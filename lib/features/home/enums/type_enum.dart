import 'package:flutter_restaurant/common/models/enum_values_model.dart';

enum Type {
  AMOUNT,
  PERCENT
}
final typeValues = EnumValuesModel({
  "amount": Type.AMOUNT,
  "percent": Type.PERCENT
});
